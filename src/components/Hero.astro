---
import { translations } from "../data/translations";
import { getNearestUpcomingShow } from "../data/shows";
import { CURRENT_YEAR } from "../utils/date";

const t = translations.ru.hero;
const nearestShow = getNearestUpcomingShow("ru");
---

<section
  id="top"
  class="relative px-3 sm:px-10 pt-12 sm:pt-20 pb-16 sm:pb-24 border-b border-rule bg-nordic-grid overflow-hidden"
>
  <!-- Atmospheric Nordic Glow Background Effect -->
  <div class="nordic-glow"></div>

  <div class="max-w-[1400px] mx-auto relative z-10">
    <!-- Top Metadata Bar -->
    <div class="flex flex-wrap items-center gap-3 sm:gap-4 mb-6 sm:mb-8 reveal">
      <span class="suomi-tag font-semibold" data-i18n="hero.countryTag"
        >{t.countryTag}</span
      >
      <span class="text-rule">/</span>
      <span
        class="font-sans text-xs uppercase tracking-[0.15em] sm:tracking-[0.2em] text-mute flex items-center gap-2"
      >
        <span class="w-2 h-2 rounded-full bg-redru inline-block"></span>
        <span data-i18n="hero.stageTag">{t.stageTag}</span>
      </span>

      <!-- Architectural Coordinates Tag -->
      <div class="hidden md:flex items-center gap-2 ml-auto text-[11px] font-mono text-mute/70 tracking-widest bg-white/60 backdrop-blur-sm px-3 py-1 rounded-full border border-rule/80">
        <span class="w-1.5 h-1.5 rounded-full bg-suomi/50"></span>
        <span>60°10'15"N · 24°57'40"E</span>
      </div>
    </div>

    <!-- Main Dynamic Hero Title -->
    <h1
      class="font-nordic font-semibold text-granite leading-[1.08] text-[7.8vw] sm:text-[5.5vw] lg:text-[5rem] xl:text-[5.6rem] tracking-tight reveal mb-8"
      data-i18n-html="hero.title"
      set:html={t.title}
    />

    <!-- Description & Hero Feature Grid -->
    <div
      class="grid lg:grid-cols-12 gap-8 lg:gap-12 mt-8 sm:mt-12 reveal items-stretch"
    >
      <!-- Description & Interactive CTAs -->
      <div class="lg:col-span-7 flex flex-col justify-between space-y-8">
        <p
          class="text-lg sm:text-2xl text-granite/90 leading-relaxed font-light max-w-2xl"
          data-i18n-html="hero.description"
          set:html={t.description}
        />

        <!-- Action Buttons Row -->
        <div class="flex flex-wrap items-center gap-4 pt-2">
          {
            nearestShow ? (
              <a
                href="#afisha"
                class="btn-suomi inline-flex items-center gap-3 px-6 py-3.5 rounded-xl font-sans text-sm font-semibold tracking-wide shadow-sm group"
              >
                <span data-i18n="hero.afishaBtn">{t.afishaBtn || "Смотреть афишу"}</span>
                <svg
                  class="w-4 h-4 arrow shrink-0 transform group-hover:translate-y-0.5 transition-transform"
                  width="16"
                  height="16"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                  stroke-width="2.2"
                >
                  <path stroke-linecap="round" stroke-linejoin="round" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
                </svg>
              </a>
            ) : (
              <a
                href="#courses"
                class="btn-suomi inline-flex items-center gap-3 px-6 py-3.5 rounded-xl font-sans text-sm font-semibold tracking-wide shadow-sm group"
              >
                <span data-i18n="courses.tag">Актерские курсы</span>
                <svg
                  class="w-4 h-4 arrow shrink-0 transform group-hover:translate-y-0.5 transition-transform"
                  width="16"
                  height="16"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                  stroke-width="2.2"
                >
                  <path stroke-linecap="round" stroke-linejoin="round" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
                </svg>
              </a>
            )
          }

          <a
            href="#about"
            class="btn-outline inline-flex items-center gap-2 px-5 py-3.5 rounded-xl font-sans text-sm font-medium tracking-wide"
          >
            <span data-i18n="hero.aboutBtn">{t.aboutBtn || "О театре"}</span>
          </a>
        </div>
      </div>

      <!-- Casting / Hiring Announcement Glassmorphism Card -->
      <div class="lg:col-span-5 flex">
        <aside aria-label="Кастинг в театр" class="w-full p-6 sm:p-7 rounded-2xl hero-card-glass flex flex-col justify-between border border-rule/90 relative overflow-hidden group">
          <!-- Subtle decorative red accent glow -->
          <div class="absolute -top-16 -right-16 w-36 h-36 bg-redru/10 rounded-full blur-2xl pointer-events-none group-hover:bg-redru/20 transition-all"></div>

          <div>
            <!-- Top Status Pill & Year -->
            <div class="flex items-center justify-between gap-3 mb-4">
              <span class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-redru/10 text-redru text-[11px] font-bold font-sans uppercase tracking-wider border border-redru/20">
                <span class="w-2 h-2 rounded-full bg-redru animate-pulse"></span>
                <span data-i18n="hero.castingTag">{t.castingTag || "Кастинг // Открытый набор"}</span>
              </span>

              <span class="font-sans text-[11px] text-mute font-medium">Logrus {CURRENT_YEAR}</span>
            </div>

            <!-- Title & Role Badge -->
            <div class="mb-3">
              <h3 class="font-nordic font-extrabold text-2xl sm:text-3xl text-granite leading-tight" data-i18n="hero.castingTitle">
                {t.castingTitle || "Ищем актёра"}
              </h3>
              <div class="mt-2 inline-block">
                <span class="inline-flex items-center px-3 py-1 rounded-lg bg-granite text-white font-nordic text-xs font-bold uppercase tracking-wider shadow-xs" data-i18n="hero.castingRole">
                  {t.castingRole || "На роль гопника"}
                </span>
              </div>
            </div>

            <!-- Subtitle Pitch -->
            <p class="font-sans text-xs sm:text-sm font-semibold text-suomi mb-2.5 leading-snug" data-i18n="hero.castingSubtitle">
              {t.castingSubtitle || "Если ты давно хотел попробовать себя на сцене — сейчас самое время."}
            </p>

            <!-- Main Description -->
            <p class="font-sans text-xs sm:text-sm text-granite/80 font-light leading-relaxed mb-5" data-i18n="hero.castingDesc">
              {t.castingDesc || "Ищем молодого парня в новую постановку театра «Логрус». Опыт не обязателен, но приветствуется!"}
            </p>
          </div>

          <!-- Bottom Contact & Action Area -->
          <div class="pt-4 border-t border-rule/80 space-y-3">
            <div class="flex flex-wrap items-center justify-between gap-2 text-xs font-sans">
              <a
                href="mailto:info@logrus.fi?subject=Кастинг%20на%20роль%20гопника"
                class="inline-flex items-center gap-2 text-granite hover:text-suomi font-medium transition-colors"
              >
                <svg class="w-3.5 h-3.5 text-suomi shrink-0" width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
                <span>info@logrus.fi</span>
              </a>

              <a
                href="tel:+358452656332"
                class="inline-flex items-center gap-1.5 text-granite hover:text-suomi font-medium transition-colors"
              >
                <svg class="w-3.5 h-3.5 text-redru shrink-0" width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                </svg>
                <span>+358 45 2656332</span>
              </a>
            </div>

            <a
              href="mailto:info@logrus.fi?subject=Кастинг%20на%20роль%20гопника&body=Здравствуйте!%20Хочу%20принять%20участие%20в%20кастинге%20на%20роль."
              class="w-full btn-suomi py-2.5 px-4 rounded-xl font-nordic text-[11px] uppercase tracking-wider font-semibold inline-flex items-center justify-center gap-2 group/btn shadow-xs cursor-pointer"
            >
              <span data-i18n="hero.castingApplyBtn">{t.castingApplyBtn || "Откликнуться на роль"}</span>
              <svg class="w-3.5 h-3.5 arrow shrink-0" width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M14 5l7 7m0 0l-7 7m7-7H3" />
              </svg>
            </a>
          </div>
        </aside>
      </div>
    </div>
  </div>
</section>
